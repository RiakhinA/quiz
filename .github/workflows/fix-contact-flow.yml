name: Fix booking contact flow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy contact patch disabled; refinements are handled by Refine quiz flow."
